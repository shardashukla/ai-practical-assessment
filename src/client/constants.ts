/** Debounce delay for task search input (ms). */
export const SEARCH_DEBOUNCE_MS = 300;

/** Delay before redirecting after successful create/update (ms). */
export const SUCCESS_REDIRECT_DELAY_MS = 800;

/** Tasks shown per page on the task list. */
export const TASKS_PAGE_SIZE = 10;

/** Recent tasks shown on the dashboard. */
export const DASHBOARD_RECENT_TASKS_LIMIT = 5;
